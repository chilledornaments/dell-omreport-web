[Unit]
Description=gunicorn for OMSA Web Redux
After=network.target

[Service]
User=omsa
Group=omsa
WorkingDirectory=/opt/omsa
Environment="PATH=$PATH:/opt/omsa/"
ExecStart=/usr/local/bin/gunicorn --workers 5 -b 127.0.0.1:5000 omsa:app